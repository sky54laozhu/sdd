# CLAUDE.md — SDD 教程系列项目宪法

## 项目身份

这是一个 **SDD（Spec-Driven Development）教程系列**，包含 29 章内容、7 个即用模板和一个完整的实战项目。本项目自身即是 SDD 方法论的实践（dogfooding）。

## 技术栈

- **内容格式**: Markdown（所有教程章节和模板）
- **示例代码**: TypeScript（Node.js runtime）
- **项目管理**: Git
- **工具链**: Claude Code、Node.js >= 18

## 写作规范

### 语言风格

- **主语言**: 中文
- **技术术语**: 保留英文原文，首次出现时附中文解释
  - 正确: "Spec（规格文档）驱动的开发方式"
  - 正确: "使用 Context Engineering 优化上下文"
  - 错误: "使用上下文工程优化上下文"（首次未给出英文）
  - 错误: "Use spec-driven development approach"（不应整句英文）
- **代码注释**: 英文
- **文件名**: 英文，kebab-case

### 章节规范

- 每章 **1500-3000 字**（不含代码块）
- 文件命名: `{两位数序号}-{英文标题}.md`（如 `01-what-is-sdd.md`）
- 章节结构:
  1. 标题与简介（一句话概括本章要点）
  2. 概念讲解（为什么 → 是什么 → 怎么做）
  3. 实战示例（可运行的代码或可复制的配置）
  4. 关键要点（3-5 条 bullet points）
  5. 练习（1-2 个动手任务）
  6. 下一章预告

### 格式约定

- 使用 ATX 标题（`#` 风格），最多四级
- 代码块必须标注语言（```typescript, ```bash, ```json 等）
- 重要概念用 `**加粗**`，术语用 `` `行内代码` ``
- 使用 `> blockquote` 表示注意事项或提示
- 表格用于对比、速查、配置参考

## 质量标准

### 必须满足

- [ ] 所有相对链接可正确跳转（无死链）
- [ ] 术语使用一致（参照 `appendix/glossary.md`）
- [ ] 模板文件可独立使用，无需阅读对应章节即可上手
- [ ] 代码示例可直接运行或复制到项目中使用
- [ ] 每章包含至少一个实战示例

### 术语一致性

核心术语固定翻译：

| English | 中文 | 说明 |
|---------|------|------|
| Spec | 规格文档 / Spec | 可互换使用 |
| Plan | 实现计划 | - |
| CLAUDE.md | 项目宪法 | 比喻性翻译 |
| Context Engineering | Context 工程 | 保留 Context 不翻译 |
| Hook | Hook | 不翻译 |
| Token | Token | 不翻译 |
| Prompt | Prompt | 不翻译 |
| Agent | 代理 / Agent | 可互换使用 |

## SDD 工作流（本项目自身）

本教程的编写遵循 SDD 原则：

```
1. Spec 阶段: 定义章节大纲、目标读者、学习目标
2. Plan 阶段: 拆解为可执行的写作任务
3. Implement 阶段: 按计划撰写内容
4. Verify 阶段: 检查质量标准、链接有效性、术语一致性
```

### 新增章节流程

1. 在对应 Part 目录创建文件（遵守命名规范）
2. 按章节结构模板编写内容
3. 更新 `README.md` 目录中的链接
4. 验证所有交叉引用链接有效
5. 确保字数在 1500-3000 范围内

### 新增模板流程

1. 在 `templates/` 创建模板文件
2. 模板必须包含:
   - 使用说明（何时使用、如何填写）
   - 完整的结构骨架
   - 占位符示例（用 `[xxx]` 标记需替换处）
3. 更新 `README.md` 模板列表

## 目录结构约定

```
sdd/
├── CLAUDE.md              ← 本文件（项目宪法）
├── README.md              ← 主索引与阅读指南
├── part-{N}-{name}/       ← 教程章节（按 Part 分组）
│   └── {NN}-{title}.md   ← 单个章节
├── templates/             ← 独立可用的模板
├── project/               ← SpecTask 实战项目
│   ├── specs/             ← 项目 Spec 文件
│   ├── plans/             ← 实现计划
│   └── src/               ← TypeScript 源代码
└── appendix/              ← 补充参考材料
```

## 编辑器行为

- 编辑 Markdown 文件时，保持已有格式风格不变
- 不要在文件末尾添加多余空行（保留一个 trailing newline）
- 代码示例中的缩进使用 2 空格
- Markdown 列表嵌套使用 2 空格缩进
